[pytest]
# The real test suite lives in tests/. Scope collection there so pytest doesn't
# pick up helper modules that merely match its *_test.py / test_* naming (e.g.
# web/mqtt_test.py, a broker-connection helper that imports paho — not a test),
# which otherwise breaks CI collection when their runtime deps aren't installed.
testpaths = tests
